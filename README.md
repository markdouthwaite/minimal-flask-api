# minimal-flask-api

## Running locally

```bash

```

```bash
bash bin/run.sh
```

## Running with `docker`

You'll need Docker installed.

```bash
docker build . -t flask-app
```

```bash
docker run -p 5000:5000 flask-demo
```
